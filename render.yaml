services:
  - type: web
    name: animation-ai
    runtime: node
    plan: free
    buildCommand: apt-get update && apt-get install -y espeak-ng && npm ci
    startCommand: node server.js
    healthCheckPath: /api/health
